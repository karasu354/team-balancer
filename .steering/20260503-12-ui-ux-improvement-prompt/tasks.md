# tasks

## Phase 1: 要件確定

- [x] UI/UX改善プロンプトの目的と責務境界を定義する（S）
- [x] `/refactor-check` との違い（品質/保守性中心 vs UI/UX中心）を明文化する（S）
- [x] 仕様不変条件（10人条件、評価重み、JSON互換性）を除外条件として記載する（S）

## Phase 2: 出力フォーマット設計

- [x] 出力項目（課題、ユーザー影響、改善案、優先度、見積もり）を固定する（S）
- [x] 判定タグ（即時対応可 / 要件化推奨）を定義する（S）
- [x] 仕様影響あり提案を `/requirements` へ誘導するルールを追加する（M）

## Phase 3: 観点定義

- [x] UIトレンド観点（情報設計、視認性、レスポンシブ、アクセシビリティ）を定義する（M）
- [x] ユーザー体験観点（操作導線、エラー復帰、入力負荷、理解コスト）を定義する（M）
- [x] 評価対象ディレクトリ（`components/`, `pages/`, `composable/`, `pages/api/`, `utils/`）を明記する（S）

## Phase 4: ドキュメント連携

- [x] API変更を伴う提案時の `docs/team-balancer.v1.yaml` 更新ルールを記載する（S）
- [x] 実装責務が `/implement` にあることを明記する（S）
- [x] 必要時のみ `.github/prompts/ui-ux-check.prompt.md` 作成へ接続できるようにする（S）

## Phase 5: UI/UX改善候補の即時対応タスク化

- [x] `pages/index.tsx` の主要CTA優先度を視覚階層で統一（S）
  - 完了条件: Primary/Secondary/Destructive の分類が適用され、主要導線が1つに明確化される
- [x] `components/*` のエラー表示を「項目直下 + 画面上部要約 + フォーカス移動」で統一（M）
  - 完了条件: バリデーション失敗時に修正箇所へ到達でき、再試行導線がUI上で明示される
- [x] `composable/api.ts` と `pages/api/*` の通信状態UI連携（処理中/成功/失敗）を統一（M）
  - 完了条件: 多重送信抑止、失敗時再試行、ユーザー通知の一貫性が確認できる
- [x] `components/*` のラベル/補助説明/必須表示を共通パターンへ寄せる（M）
  - 完了条件: フォーム部品間でラベル関連付けと説明表示が一貫する
- [x] `pages/*` のモバイル操作完了性を改善（主要操作到達性、スクロール負荷）する（M）
  - 完了条件: モバイルで主要操作までの到達ステップを削減できる
- [x] `components/*` のキーボード操作と `focus-visible` を統一（S）
  - 完了条件: Tab移動時のフォーカス可視性と Enter/Escape 操作の挙動が揃う
- [x] `pages/index.tsx` の用語を平易化し、必要箇所へ補足説明を追加（S）
  - 完了条件: 初見ユーザーが主要文言を理解しやすく、次アクションが明確になる
