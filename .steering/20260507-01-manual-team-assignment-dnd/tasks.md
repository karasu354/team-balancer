# tasks

## Phase 1: 要件確定（S）

- [x] 手動分割モードの UX 仕様（モード切替、初期表示、確定条件）を確定する（S）
- [x] DnD 操作の許可範囲（未配置→枠、枠→枠、枠→未配置）を確定する（S）
- [x] API 互換性の扱い（初期は API 非変更）を明文化する（S）

## Phase 2: ドメインロジック（utils/）

- [x] `utils/teamBalancer.ts` に手動配置検証関数を追加する（M）
- [x] 10人条件・重複配置・未配置チェックの戻り値型を定義する（S）
- [x] 既存自動分割ロジックに影響を与えないことを確認する（S）

## Phase 3: ユニットテスト（test/utils/）

- [x] 手動配置検証関数の正常系テストを追加する（M）
- [x] 境界値（9人 / 10人 / 11人）テストを追加する（M）
- [x] 重複配置・欠員配置・無効入力の異常系テストを追加する（M）

## Phase 4: UI 実装（components/, pages/）

- [x] 分割エリアに自動/手動モード切替 UI を追加する（M）
- [x] 手動分割ボード（10ロール枠 + 未配置プレイヤー）を実装する（L）
- [x] プレイヤーカードの DnD 操作（ドラッグ開始/ドロップ/キャンセル）を実装する（L）
- [x] ドロップ先ハイライトとエラー表示を実装する（M）
- [x] 自動分割結果を手動分割初期値として取り込む導線を実装する（M）

## Phase 5: E2E テスト（e2e/）

- [x] 手動モードへの切替と表示確認テストを追加する（M）
- [x] DnD による配置変更と確定成功テストを追加する（L）
- [x] 10人未満/重複配置/未配置で確定失敗するテストを追加する（M）
- [x] 自動モード回帰（既存分割フロー維持）テストを追加・更新する（M）

## Phase 6: ドキュメント更新（docs/）

- [x] `docs/team-balancer-spec.md` に手動分割機能と DnD UX を追記する（M）
- [x] API 契約変更の有無を確認し、必要時のみ `docs/team-balancer.v1.yaml` を更新する（S）

## Phase 7: 最終検証

- [x] `npm run format` を実行（S）
- [x] `npm run format:test` を実行（S）
- [x] `npm run typecheck` を実行（S）
- [x] `npm run test` を実行（M）
- [x] `npm run test:e2e` を実行（M）

## Phase 8: 完了条件

- [x] DnD による手動ロール割り当てが可能である（M）
- [x] 自動分割と手動分割を切り替えて利用できる（M）
- [x] 10人条件と既存互換性（fromJson / playersInfo）を壊していない（S）
- [x] requirements / design / tasks の整合が取れている（S）

## Phase 9: UI/UX 改善（実装後レビュー指摘対応）

### 9.1 入力プレビュー配置調整

- [x] `pages/index.tsx` のコンポーネント配置順を見直し、プレビューを確定ボタン直前に移動（M）
- [x] PlayerInputForm の出力（入力状態）を DividedTeamTable へ props として渡す際の親子関係を確認（S）
- [x] レイアウト変更後の E2E テストを実行確認（M）
- [x] ビジュアルテスト（入力 → プレビュー → 確定 の流れが自然か）（S）

### 9.2 プレイヤー一覧カード表示形式の再検討

- [x] 現行カード型表示（components/PlayersTable.tsx）のレイアウト特性を整理（S）
- [x] コンパクト行型 / テーブル型 等の代替案を検討し、モック/プロトタイプを作成（M）
- [x] 情報密度とスクロール性能のバランスを検証（M）
- [x] UI デザイン確定後、コンポーネント実装（L）
- [x] 関連コンポーネント（BulkEditRow, PlayerDeleteCard）との統一性を確認（M）
- [x] E2E / Unit テストの更新（M）

### 9.3 手動分割モードでのチームバランス数値表示

- [x] 手動分割完了時の UI に「チーム評価値」表示領域を追加（M）
- [x] `utils/teamBalancer.ts` の既存評価計算ロジック（`calculateTeamScore` 等）を確認し、手動配置結果へ適用（S）
- [x] 自動分割モード同様の「ロール数内訳」（各ロール人数）を表示（S）
- [x] `DividedTeamTable.tsx` 手動モード表示部に数値表示セクションを追加実装（M）
- [x] E2E で手動配置後のチームバランス表示が正確であることを確認（M）

### 9.4 試合履歴パネルのダークテーマ適用

- [x] `MatchHistoryPanel.tsx` / `MatchHistoryAccordion.tsx` で使用されている色・スタイルを確認（S）
- [x] CSS custom properties（--tb-surface, --tb-text-primary, --tb-accent など）の適用状況を確認（S）
- [x] 不足しているダークテーマ適用箇所を特定（例: テキスト、背景、ボーダー、アイコン）（M）
- [x] ダークテーマ対応への色定義を追加・修正（M）
- [x] ビジュアルテスト（light/dark 両モードで確認）（M）

## Phase 9 完了条件

- [x] 入力プレビューがプレイヤー追加直前に表示される（M）
- [x] プレイヤーカード表示形式が再設計・実装されている（L）
- [x] 手動分割モードでチームバランス数値が表示される（M）
- [x] 試合履歴セクションがダークテーマで統一されている（M）
- [x] 全 E2E / Unit テスト成功、回帰がない（M）
- [x] `npm run format`, `npm run typecheck` 成功（S）
